image: registry.gitlab.com/4tu/spheal

variables:
  GIT_SUBMODULE_STRATEGY: recursive

.common: &common
  script:
    - make -f Makefile.$PLATFORM
  artifacts:
    paths:
      - vgedit.$EXT

PC:
  <<: *common
  variables:
    PLATFORM: pc
    EXT: bin

WiiU:
  <<: *common
  variables:
    PLATFORM: wiiu
    EXT: rpx

Switch:
  <<: *common
  variables:
    PLATFORM: switch
    EXT: nro
